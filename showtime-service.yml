spring:
  application:
    name: showtime-service
  profiles:
    active: dev
server:
  servlet:
    context-path: /showtime
  port: 9080
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9010/eureka/
feign:
  circuitbreaker:
    enabled: true
